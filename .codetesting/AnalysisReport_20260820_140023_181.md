# Test Failures due possible code bugs

## DriversRoutesUnitTest.csproj - GetCustomerRoutesRouteId
- **Confidence**: High
- **Test File**: DriversRoutesUnitTest\API\Get\GetCustomersHttpTest.cs
- **Bug Location**: DriversRoutes\Data\RouteApi\GetCustomersHttp.cs@56-78

### Analysis
The test is failing with a 401 Unauthorized error when calling GetCustomerRoutes. The test setup properly initializes an authenticated user and JWT token via CustomWebApplicationFactory, and configures UserAfterLogin.SetLoginUser(token) at line 104. However, the GetCustomersHttp.GetCustomerRoutes method at line 76 makes an HTTP request via httpClient.DownloadAsync() without adding the Authorization header. Recent changes to the production code likely removed or broke the middleware/logic that automatically adds the JWT bearer token to outgoing HTTP requests. The HttpClient.GetAsync() call at HttpClientExtension.cs line 22 needs to include the JWT token from the UserAfterLogin context in the request headers as 'Authorization: Bearer {token}'.

### Suggested Fix
The GetCustomersHttp class needs to be fixed to include the JWT authentication token in the HTTP requests. In the GetCustomerRoutes method (around line 56-78), before calling `httpClient.DownloadAsync()`, the JWT token from `UserAfterLogin` should be added to the HttpClient's default request headers. This can be done by: (1) Creating or modifying an HttpClient interceptor/handler to automatically add the Authorization header from UserAfterLogin context, or (2) Explicitly adding the token to the request headers in the GetCustomerRoutes method before making the DownloadAsync call. The fix should add: `httpClient.DefaultRequestHeaders.Authorization = new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", tokenValue);` where tokenValue is retrieved from the UserAfterLogin static context. Alternatively, if there's a message handler chain, ensure it includes the authentication handler that was present before the recent changes.

## DriversRoutesUnitTest.csproj - SendCustomerRoute
- **Confidence**: Medium
- **Test File**: DriversRoutesUnitTest\API\Send\SendCustomersHttpTest.cs
- **Bug Location**: 

### Analysis
The test is failing at line 47 with `Assert.True(result.IsSuccessStatusCode)` returning false. This is an integration test that sends a modified CustomerRoute to the server via HTTP POST. The production code in SendCustomersHttp.SendCustomerRoute method (line 22-42) constructs the URL correctly with the forceUpdate parameter, serializes the object using SzarotkaJsonSerializerContext, and sends it via PostWithProgressAsync. The fact that this was a previously passing test that became failing after recent changes suggests the issue is likely in the HTTP endpoint handling the `/driver-routes/update` POST request, not in the SendCustomersHttp class itself. However, one possibility is that the recent changes modified the JSON serialization context or the object state in a way that causes the server to reject the request. The test correctly uses the same object instance for comparison, so the JSON comparison at line 48 should pass if the send succeeds. The root cause is most likely that the backend API endpoint is now returning a non-success status code when processing the updated CustomerRoute.

### Suggested Fix
The issue is likely not in SendCustomersHttp.cs itself, but in the backend API endpoint that handles POST requests to '/driver-routes/update'. The endpoint should be checked to ensure it correctly processes the CustomerRoute object with the forceUpdate=true parameter. Without access to the backend API code, the most likely culprits are: 1) The endpoint logic was changed and now rejects valid requests, 2) Database constraints were modified that prevent the update, 3) The JSON deserialization on the server side doesn't match the SzarotkaJsonSerializerContext format used by the client. The fix requires examining the backend API controller/endpoint and its associated services to identify why it returns a non-success HTTP status code.

## DriversRoutesUnitTest.csproj - SendCustomerRoutes
- **Confidence**: Medium
- **Test File**: DriversRoutesUnitTest\API\Send\SendCustomersHttpTest.cs
- **Bug Location**: DriversRoutes\Data\RouteApi\SendCustomersHttp.cs@44-64

### Analysis
The test is failing because the HTTP request in SendCustomerRoutes() is returning a non-success status code. The test flow is correct: it retrieves routes, modifies them, sends them via HTTP, and verifies the response status. Since this was previously passing and recently became failing after changes to the tested code, the issue is likely in the production code's HTTP endpoint handling or the serialization. The most probable cause is either: (1) The endpoint URL structure changed, (2) The serialization context (SzarotkaJsonSerializerContext.Default.IListCustomerRoutes) is not properly configured, or (3) The server endpoint implementation has an issue. The test expects a success status code but receives a failure.

### Suggested Fix
The issue is in the SendCustomerRoutes method in SendCustomersHttp.cs. Verify that: (1) The endpoint URL '/driver-routes/updates' is correctly implemented on the server and accepts POST requests, (2) The JsonSerializer context 'SzarotkaJsonSerializerContext.Default.IListCustomerRoutes' correctly serializes the IList<CustomerRoutes> object, (3) The HTTP client configuration is correct. If the endpoint was recently renamed or changed, update line 46 to use the correct endpoint URL. If the serialization context is missing or misconfigured, verify that it includes support for serializing IList<CustomerRoutes>.

